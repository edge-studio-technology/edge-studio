# Backend Unit Tests Plan

**Status:** In Progress
**Created:** 2026-07-22
**Goal:** Build vitest unit test coverage across `backend/src/features/*`, one feature folder at a time. Prioritize security-critical and business-critical logic over exhaustive coverage — pure-logic and DB-backed service/repository behavior first, thin route wiring last (or skipped where it's just glue).

**Scope:** `backend/src/features/*` (see Progress table) plus `backend/src/shared/*` (cross-cutting, tracked as its own row below). Out of scope: `config/` (`env.ts`/`loadEnv.ts` — env parsing with no branching logic worth testing), `middleware/requestLogger.ts` (thin logging glue), `db/database.ts`/`db/ensureDatabaseDirectory.ts` (schema/migrations exercised indirectly by every DB-harness test; a dedicated migration test can be added later if migrations grow more complex than additive `CREATE TABLE IF NOT EXISTS`/`ALTER TABLE`).

## Conventions

- Test files live in `backend/tests/features/<feature>/<module>.test.ts`, mirroring `backend/src/features/<feature>/<module>.ts`.
- Use `node:assert/strict` + vitest's `describe`/`it` (not vitest's `expect`) — matches the pre-existing `minima.parse.test.ts`/`tokens.parse.test.ts`.
- Import source with the compiled `.js` extension (NodeNext ESM resolution), e.g. `../../../src/features/auth/password.service.js`.
- Pure-function modules (parsing, validation) need no setup.
- DB-backed modules (repositories, services that hit `db`) use `backend/tests/helpers/testDatabase.ts` — sets `DATABASE_PATH` to a unique temp file, dynamically imports `db/database.js` so the singleton binds to it, runs `runMigrations()`, and returns `{ db, teardown }`. Dependent modules (repository/service) must also be dynamically imported inside `beforeAll`, after the DB is set up, since they statically import the `db` singleton.
- Network-calling modules: mock at the boundary closest to the module under test, not further down the stack.
  - For the module that owns the actual `fetch` call (e.g. `minima.rpc.ts`), stub `global.fetch` with `vi.stubGlobal("fetch", vi.fn())` in `beforeEach`/`afterEach` (`vi.unstubAllGlobals()` after) — this exercises the module's own URL-building/parsing logic for real, only the network I/O is faked.
  - For modules one level up that *depend on* an already-tested lower module (e.g. `minima.service.ts` depends on `minima.rpc.ts`/`minima.docker.ts`), mock the dependency module itself with `vi.mock("<relative-path-from-the-test-file>.js", factory)`. Because `vi.mock` calls are hoisted above imports, any mock function referenced in the factory must be declared via `vi.hoisted(() => ({ ... }))` first, or you'll hit a "Cannot access before initialization" error.
  - Modules with module-level mutable state (e.g. `minima-monitoring.ts`'s in-memory snapshot) should `vi.resetModules()` and re-`import()` fresh in `beforeEach` so tests don't leak state into each other; this also gives a clean way to re-read `env` after changing `process.env` for a specific test (see `minima-poll.service.test.ts`'s `MINIMA_AUTO_RESYNC` toggle).
  - Time-dependent logic (cooldowns, expiry) uses `vi.useFakeTimers()` + `vi.setSystemTime()`, restored with `vi.useRealTimers()` in `afterEach`.
- Routes (`*.routes.ts`) are **not** unit-tested directly. Decision: treat them as thin wiring — pull input off `req`, call an already-tested service function, map the result/error to a response — and consider service/repository coverage sufficient. Revisit for a specific route only if it grows real logic beyond that. See "Future Hardening" for the gap this leaves.

## Progress

| Feature | Status | Notes |
|---|---|---|
| `auth` | Done | Covered: `password.service.ts`, `session.service.ts`, `auth.service.ts` (`login`/`changePassword`), `auth.repository.ts` setup-pending lifecycle, `audit.service.ts`, `auth.middleware.ts` (`requireAuth`/`requireRole`), `setup.service.ts` (admin-creation/setup-complete state machine, `completeSetup`, guarded TOTP error paths). Deliberately skipped: `totp.service.ts` (dead code, `TOTP_ENABLED = false`) and the happy-path "valid TOTP code" branches of `verifySetupTotp`/`initSetupTotp` (same reason — not worth the investment). Also skipped: `rate-limit.middleware.ts` (trivial `express-rate-limit` config, no logic) and `integritas-validation.service.ts` (fully commented out, no live exports). `auth.routes.ts`/`setup.routes.ts` intentionally out of scope — see the routes decision above and "Future Hardening" below. |
| `minima` | Done | Covered: `minima.parse.ts` (pre-existing), `minima.errors.ts` (pure), `minima.rpc.ts` (global `fetch` stubbed via `vi.stubGlobal` — established the network-mocking pattern), `minima-monitoring.ts` (stall detection, cooldown via `vi.useFakeTimers`/`vi.setSystemTime`, snapshot state via `vi.resetModules()` per test), `minima.docker.ts` (`getMinimaContainerStats` with `../status/docker.service.js` mocked via `vi.mock`, `getMinimaStorageInfo` pure), `minima.service.ts` (full orchestration — `getMinimaNodeStatus`'s state derivation, block/peers fallback-and-failure paths, config, peers/wallet/resync/restart — with `minima.rpc.js`/`minima.docker.js`/`status/docker.control.js` mocked via `vi.mock`+`vi.hoisted`, DB harness for settings), `minima-poll.service.ts` (`pollMinimaHealth`'s concurrency guard, stall/no-stall, auto-resync gate/cooldown/success/failure — mocking `minima.service.js`/`minima-monitoring.js`, `vi.resetModules()` + `process.env.MINIMA_AUTO_RESYNC` to flip `env` per describe block). Not covered: `minima.routes.ts` — out of scope per the routes decision above. |
| `tokens` | Done | Covered: `tokens.parse.ts` (pre-existing), `tokens.repository.ts` (DB harness), `tokens.service.ts` (`wallet.service.js`/`minima.rpc.js` mocked via `vi.mock`+`vi.hoisted`; validation, insufficient-balance short-circuit, RPC failure, Minima error mapping, create+dedupe, `listWalletTokens` merge/fallback). `tokens.routes.ts` intentionally out of scope. |
| `automation` | Done | Covered: `automation.validation.ts` — `dataSources.repository.js`/`dataSources.service.js`/`address-book.repository.js`/`settings/secrets.service.js`/`wallet.service.js`/`data-sources/cameraCapture.service.js` mocked via `vi.mock`+`vi.hoisted`; `validateAutomationDraft` covered exhaustively (workflow shape, set_variable, condition, show_preview, block references, attached stamp blocks, camera capture, send_transaction balance checks), plus `validateAutomationWorkflow` via the DB harness with real `automation.repository.ts`. `automationRuns.repository.ts` (runs + block runs, list/count/filter/pagination) and `automationInbox.repository.ts` (create/get/read/delete, list/count filtering) covered via the DB harness, using real `automation.repository.ts` workflows/blocks to satisfy FK constraints. `automation.repository.ts` itself covered via the DB harness — workflow/block CRUD, ordering/reordering, `duplicateAutomationWorkflow`, run/block-run status updates, `listDueScheduleWorkflows`/`listEnabledEventWorkflows`/`getEnabledAutomationWorkflowForDataSource`/`listAutomationWorkflowsUsingDataSource`. `automation.service.ts` (the orchestration/execution engine) covered via the DB harness with real `automation.repository.ts`/`automationRuns.repository.ts`/`automationInbox.repository.ts`/`dataSources.repository.ts`/`dataReads.repository.ts`/`integritas.repository.ts`/`address-book.repository.ts`/`auth/audit.service.ts`, and `data-sources/dataSources.service.js`/`data-sources/sensorHelper.service.js`/`data-sources/gpioOutput.service.js`/`data-sources/mqttOutput.service.js`/`data-sources/cameraCapture.service.js`/`integritas/integritas.service.js` (partial mock, real error-classifier functions kept)/`settings/secrets.service.js`/`wallet/wallet.service.js` (partial mock, real `recordWalletSendHistory`/`listWalletSendHistory` kept) mocked via `vi.mock`+`vi.hoisted`. Exercises: run guards (not found/archived/disabled/no-blocks/start-type-mismatch/source-mismatch/event-inactive/cooldown/already-running), success and failure run lifecycle with structured error persistence, schedule next-run computation, attached-block execution order, `if_payload_field_equals` stop/continue, `set_variable` all four sources, `wait` bounds, `record_trigger_event`, `recordPushAutomationPayload`, `fetch_data_source` success/failure, `capture_camera` success/failure, `show_preview` content modes/formats, `stamp_integritas` (missing hash/API key, transient/unauthorized/payment-required/generic stamp failures, success, condition skip), `control_output` (gpio/http/mqtt/multipart targets, missing/invalid target), `send_transaction` (validation, balance checks, success/failure), and the schedule poller (idempotent start, due-workflow execution via fake timers). |
| `wallet` | Done | Covered: `wallet.parse.ts` (pure — balance/address/send/payment-status/import response parsing incl. native-token naming, custom-token metadata fallback, txpowid nested-fallback, confirmation flags). `wallet.service.ts` — `minima/minima.rpc.js` mocked via `vi.mock`+`vi.hoisted`; `getWalletStatus`/`getReceiveAddress` (incl. RPC-not-ok throw, generated QR data URL)/`sendPayment` (address/amount validation, RPC command string, default tokenId)/`getPaymentStatus`/`importWallet` (RPC-not-ok throw, phrase in restore command), plus DB-backed `clearWalletSendHistoryForDebug`/`recordWalletSendHistory`/`listWalletSendHistory` (ordering, limit clamping) via the DB harness. `wallet.routes.ts` intentionally out of scope. |
| `data-reads` | Done | Covered: `dataReads.repository.ts` via the DB harness (real `dataSources.repository.ts`/`integritas.repository.ts` for FK-satisfying source/proof rows) — create defaults, preview/triggerPayload JSON serialization, structured/plain-string error storage, proof linking, status/q filtering, pagination, DESC ordering. `dataReads.service.ts`'s `serializeDataSourceRead` (pure) — snake_case-to-camelCase mapping, null vs. parsed JSON preview/triggerPayload, structured vs. legacy plain-string error mapping. `dataReads.routes.ts` intentionally out of scope. |
| `data-sources` | Partial | Covered: `dataSources.repository.ts` via the DB harness (create/get/list ordering, `findWebhookDataSource` incl. malformed-config JSON, update/delete, `updateDataSourceReadResult` hash/preview/structured-and-plain error). `dataSources.service.ts` — `serializeDataSource` (pure), every `parse*Config` function (defaults, trimming, validation errors) and `parseDataSourceConfig` dispatch, `process*Payload` helpers (pure), `readDeviceSystemDataSource` (config-flag section toggling), `checkDataSourceHealth`/`readJsonApiSource`/`sendHttpOutput`/`sendMultipartMediaOutput` with `global.fetch` stubbed via `vi.stubGlobal`. Not covered: `gpioIngestion.service.ts`/`gpioOutput.service.ts`/`mqttIngestion.service.ts`/`mqttOutput.service.ts`/`cameraCapture.service.ts`/`sensorHelper.service.ts` (hardware/MQTT/host-helper mocking) and `dataSources.routes.ts` (out of scope per the routes decision). |
| `integritas` | Done | Covered: `integritas.repository.ts` via the DB harness (create/get, count/list filtering and pagination, `countPollablePendingProofRecords`/`listPendingProofRecords` pending+proof_uid semantics incl. a genuinely-NULL `proof_uid` row, `updateProofStatus` COALESCE-vs-always-overwrite column behavior, `updateVerifyResponse`, `deleteProofRecords`). `integritas.service.ts` — pure functions (`hashCanonicalBytes`, `proofPayloadFromStatusItem` incl. all onchain truthy encodings, `isTransientIntegritasErrorCode`/`isIntegritasUnauthorizedErrorCode`, `parseProofPayload`, `isProofPollExpired`); DB-backed (`expirePendingProofIfTimedOut`, `applyPollResultToRecord` incl. uid normalization, `refreshProofRecord`); `global.fetch` stubbed via `vi.stubGlobal` for `requestProofUid`/`pollProofStatus`/`verifyProof` incl. the shared retry/backoff path (transient 5xx/429/AbortError retries via `vi.useFakeTimers`+`vi.advanceTimersByTimeAsync`, non-transient failures short-circuit, Retry-After header propagation); `sha3HashFile`, `writeProofExport`, `writeProofSourceZip` (JSON-preview and Raspberry Pi Camera media-bytes hashing paths, hash-mismatch and missing-file failure paths) exercised against a real temp `DATA_DIR`. `integritas-poll.service.ts` — `pollPendingProofRecords`/`startIntegritasProofPoller`/`stopIntegritasProofPoller` covered with `settings/secrets.service.js`/`integritas.repository.js`/`integritas.service.js` mocked via `vi.mock`+`vi.hoisted` (no-API-key/no-pending short circuits, batch polling and per-record `applyPollResultToRecord`, expiry-filtered batches, per-record error isolation, concurrency guard, interval start/stop/idempotent-start). `integritas.routes.ts`/`upload.middleware.ts` intentionally out of scope per the routes decision. |
| `integritas-auth` | Done | Covered: `integritas-auth.repository.ts` via the DB harness (activation/`integritas_auth`/account-cache CRUD, `clearIntegritasConnectState`, `markConnectRevoked`'s terminal-row semantics). `integritas-auth-crypto.service.ts` (pure round-trip, non-deterministic ciphertext, malformed-input throw). `integritas-auth-account-cache.ts` (pure `sanitizeMeForCache`/`parseAccountCache`/`accountCacheHasDevices`; DB-backed `getCachedProfile`). `integritas-auth-device-identity.service.ts` — `status/device.service.js` mocked via `vi.mock`+`vi.hoisted` to control arch/platform (device creation, settings.device_id reuse/drift-resync, linux-arm* → `raspberry_pi` vs. everything else → `self_hosted`). `integritas-auth-client.service.ts` — `global.fetch` stubbed via `vi.stubGlobal` for `startActivation`/`getActivationStatus`/`getMe`/`refreshToken` incl. the shared response envelope's error mapping (`success:false`, non-ok HTTP, AbortError timeout, malformed success shape). `integritas-auth-token-manager.service.ts` — `integritas-auth-client.service.js` mocked via `vi.mock`+`vi.hoisted`, real repository/device-identity/crypto via the DB harness (`assertStoredTokensDecryptable`, `getValidAccessToken`'s no-refresh/near-expiry-refresh/unparseable-expiry paths, DEVICE_REVOKED handling, generic-error wrapping, shared in-flight-refresh concurrency guard). `integritas-auth.service.ts` — client + token-manager mocked via `vi.mock`+`vi.hoisted`, real repository/account-cache via the DB harness (`startConnectActivation`, `getIntegritasAuthStatus`'s full activation-polling state machine incl. approved/denied/expired/connected/missing-remote-activation branches and `completeApprovedActivation`'s success/non-fatal-error/DEVICE_REVOKED paths, `getUserProfile`'s cache-hit/fetch/force-refresh/stale-fallback/fatal-rethrow paths). `integritas-auth.routes.ts` intentionally out of scope per the routes decision. |
| `settings` | Done | Covered: `settings.repository.ts` via the DB harness (`saveSetting` insert/overwrite-on-conflict/isolation between keys, `getSetting` missing-key default, `deleteSetting` existing/no-op). `secrets.service.ts` — `getConnectedIntegritasApiKey`/`getIntegritasApiKey`/`integritasApiKeySource` via the DB harness with real `integritas-auth.repository.ts`/`integritas-auth-crypto.service.ts` (no row, null `api_key_enc`, valid decrypt, malformed-ciphertext catch-and-empty). |
| `status` | Done | Covered: `device.service.ts` via the DB harness with real `settings.repository.ts` (`ensureDeviceId` create-once/no-overwrite, `getDeviceInfo` real OS facts + stored id + disk fallback). `docker.service.ts` — `node:http`'s `request` mocked via `vi.mock`+`vi.hoisted` at the boundary it owns (`getComposeServiceContainer` project/service label filtering, non-2xx/transport-error rejection, `inspectContainer`, `dockerServiceResources` running-vs-stopped stats skip and cross-project filtering); `diskUsage` exercised against a real temp dir (no mocking needed). `docker.control.ts` — `docker.service.js` mocked via `vi.mock`+`vi.hoisted` (its dependency) plus `node:http` mocked directly for the POST calls it owns itself (`restartComposeService`/`startComposeService` not-found guard, success, HTTP-304-as-success, non-2xx rejection; `getContainerRestartBaseline`; `waitForContainerRestart`'s RestartCount/StartedAt change detection, timeout, and transient-inspect-failure resilience). `status.routes.ts` intentionally out of scope per the routes decision. |
| `address-book` | Done | Covered: `address-book.repository.ts` via the DB harness — `insertAddressBookEntry` (generated id/timestamp, null notes), `listAddressBookEntries` case-insensitive label ordering, `getAddressBookEntryById`/`getAddressBookEntryByAddress` found/not-found, `updateAddressBookEntry` (not-found, partial-field updates, notes-to-null vs. notes-untouched, address update), `deleteAddressBookEntry` found/not-found. `address-book.types.ts` is pure types; `address-book.routes.ts` intentionally out of scope per the routes decision (validation logic there is thin request-shape guards over `isMinimaAddress`, itself covered under the `shared` row). |
| `feedback` | Done | Covered: `feedback.remote.ts` — `global.fetch` stubbed via `vi.stubGlobal` for `sendHostedFeedback` (success incl. `remoteId`/`receivedAt` omission, 200-with-`ok:false` non-retryable rejection, 5xx/429 retryable, other 4xx non-retryable, plain-text error body fallback, network/abort errors retryable, `remoteDelivery` stripped from the outgoing payload). `feedback.service.ts` — `integritas-auth.repository.js`/`minima.service.js`/`settings/secrets.service.js`/`status/device.service.js`/`feedback.remote.js`/`shared/http.js` mocked via `vi.mock`+`vi.hoisted`, real `db` via the DB harness with a real temp `DATA_DIR` (same pattern as `integritas.service.test.ts`) for file I/O and `countRows` stats. Exercises `getFeedbackExportPath`, `getFeedbackConfig`, `getEmptyFeedbackDocument`, `appendFeedbackSubmission` (full input validation incl. bug/feature-request sub-validation and defaults, hosted-consent gate, no-API-key local-only path, sent/pending/failed hosted delivery outcomes, node/integritas operational-status success/failure/throw branches), `retryPendingFeedback` (no-API-key skip counting, per-submission retry with mixed outcomes, already-sent submissions skipped), and `getFeedbackExport` (empty-document fallback vs. persisted file). |
| `files` | Not started | `files.service.ts` — host filesystem access, needs a fixture/sandbox strategy. |
| `debug`, `health` | Not started | Thin route wiring — likely low priority, may skip. |
| `shared` | Not started | Cross-cutting, not feature-scoped, but in scope for this plan. `crypto.ts` (`encryptSecret`/`decryptSecret`, `APP_SECRET`-keyed — security-critical, used by Integritas token storage and Minima backup password) is the highest-priority target. `list-query.ts`, `minima-address.ts`, `format.ts` are pure-function modules, cheap to cover. `api-error.ts`/`structured-error.ts` are mostly shape/mapping helpers — check for real branching logic before scoping. `http.ts` needs `fetch` mocking if it owns request logic. |

## Future Hardening

Routes are deliberately unit-test-free (see the decision above), which means nothing today directly verifies HTTP-layer-only behavior: per-route input guards (e.g. `typeof req.body?.password === "string" ? ... : ""`), error-to-status-code mapping in each route's `catch` block, cookie serialization (`res.cookie(name, token, sessionCookieOptions())`), and — the one that actually matters for security — that `requireAuth`/`requireRole` are wired onto the right routes in `app.ts`. A route unit test would catch these; a service test structurally cannot.

Considered and rejected for now: full supertest coverage per route file (`*.routes.test.ts` mirroring every route module) — high cost (new `supertest` dependency, real middleware stack per request, `authRateLimiter`'s in-memory 5-req/15min limiter needs a reset/mock strategy to avoid tests 429ing each other) for low marginal value once handlers stay thin.

Cheaper alternative, not yet started: a single supertest-based smoke test that walks `app.ts`'s route table and asserts every non-public `/api/*` route returns 401 without a session cookie (public routes per `backend.md`: `GET /api/health`, `GET /api/setup/status`, `POST /api/setup/*`, `POST /api/auth/login`). Written once, catches the one failure mode — a route missing `requireAuth` — that matters most, without turning every route file into an integration-test target.

## Verification

- `npm --prefix backend run test` (vitest)
- `npm --prefix backend run typecheck`

Keep this file updated as work progresses — flip rows to Partial/Done and resolve Open Questions as decisions are made.
