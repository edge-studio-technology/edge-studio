# Session

Scratch log for the session in progress. Update it as you go; reset it when a session's work is done and merged. Not a changelog — see `CHANGELOG.md` for user-facing history.

## Progress

Branch `ui/pre-release-improvements`. (Previous log entries here were for `ui/global-style-realignment`, already merged via PR #64; reset below per this file's own reset-when-merged rule.)

- Found a previously-reverted change on this branch's history (`1138652` "Realigned the Automation workflows table with ESDS patterns", reverted 19 minutes later by `14b373e`, both from 2026-08-10) and reimplemented it now that the branch is stable. The original diff predated several later refactors (workflow rename, `workflowWorkspaceUi.tsx` extraction, `AutomationInboxPanel.tsx` extraction) and referenced ESDS components (`TableCard`, `ListFilterBar`, `ListPaginationFooter`, `EmptyContentState`, `LoadingState`, `TableIconMenu`, `Pill`, `TruncatedHash`, `DeleteConfirmModal`/`DeleteProgressModal`) that didn't exist in the codebase at merge time but do now (added since via the `ui/global-style-realignment` branch), so reapplied the same intent onto the current file structure rather than cherry-picking the old diff directly.
- Extracted a new `frontend/src/features/automation/AutomationWorkflowsList.tsx`, modeled directly on `features/data-sources/DataSourcesList.tsx` (the current canonical example of this list pattern): `TableCard` shell, `ListFilterBar` (status filter + search) with "Create new workflow" button beside it, `LoadingState`/`EmptyContentState` replacing the table entirely during first load / when empty (instead of as a row inside it), a `table-fixed` `DataTable` with `TruncatedHash` for the hash column and `formatLocalDateTime` for "Last run", row actions collapsed to a primary pause/play `TableIconButton` plus a `TableIconMenu` overflow (Run now, Open and edit, Watch workflow, Duplicate, Archive/Restore, Delete), and a `ListPaginationFooter`. Reused the existing `WorkflowStatusPill`/`workflowMatchesFilter`/`summarizeBlocks`/`workflowIntervalSeconds`/`formatInterval`/`workflowPrimarySourceId` helpers rather than duplicating their logic.
- Updated `pages/AutomationPage.tsx` to use the new component: removed the separate "Workflow builders" card (its button now lives in the list's filter-bar row) and the old raw `<table>` markup; wired the workflow delete flow onto the shared `DeleteConfirmModal`/`DeleteProgressModal` pattern (`deleteTarget` for the confirm step, `deletingWorkflow` for the progress step), matching `DataSourcesPage.tsx`'s existing device-delete flow, replacing the previous immediate-delete-on-click button. Added a one-time `workflowsLoading` state (true until the first `refresh()` resolves) passed through as the list's `loading` prop. Search/filter/pagination state moved out of the page and into the new list component (page no longer needs `workflowSearch`/`workflowFilter`/`filteredWorkflows`), pruning now-unused imports (`Archive`/`Copy`/`Eye`/`Pencil`/`Play`/`RotateCcw`/`Trash2` icons, raw `DataTable`/`RowActions`/`TableWrap`/table class-name exports, `IconAction`/`StatusPill`/`WorkflowStatusPill`/`cardClass`-adjacent workspace-UI helpers no longer used by the page itself, `cx`, `formatLocalTime`, and several now-unused `workflowHelpers` exports) — `cardClass`/`mutedText`/`workflowPrimarySourceId` are kept since the page's workspace-loading fallback and `sourceById` lookup still use them.
- Verified with `npm run typecheck` (clean), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. `npm run check`'s `audit:moderate` step fails, but only on pre-existing transitive-dependency advisories (`body-parser`, `brace-expansion`, `esbuild`, `ip-address`, `multer`, `tar`), unrelated to this change and present before it. `CHANGELOG.md` updated under a new `## [Unreleased] ui/pre-release-improvements` section. No manual browser check yet (no browser-automation tool available in this environment).
- User then asked for two follow-ups: make the inbox full width, and pass over the whole Automation page again to catch any remaining ESDS drift, suggesting a new table-based inbox component. Recommended (before building) collapsing the inbox onto the exact same `TableCard`/`ListFilterBar`/`ListPaginationFooter`/`EmptyContentState`/`LoadingState`/`TableIconMenu` pattern just built for the Workflows table, with the always-inline preview moved into a "View details" modal like the Diagnostics history tables — user confirmed ("proceed").
- Root cause of "not full width": `Page`'s outer flex container uses `items-start`, so any direct child without an explicit `w-full` shrinks to its content width instead of stretching — `AutomationWorkflowsList`/`DataSourcesList` already set `className="w-full"` on their `TableCard`, but the old `AutomationInboxPanel`'s bare `<section className={cardClass}>` never did, which is exactly why it looked narrower than the page.
- Deleted `features/automation/AutomationInboxPanel.tsx` (stacked-card layout, always-inline preview, `cardClass`/`statusRowClass`/`softCardClass`/raw `<button>`/`<a>` styling) and added `features/automation/AutomationInboxTable.tsx` in its place, mirroring `AutomationWorkflowsList.tsx`'s structure: `TableCard` (`w-full`, unread-count `Pill` in the header via `TableCard`'s `actions` slot), a status filter (All/Unread/Read) + search (title/workflow/format) via `ListFilterBar`, `LoadingState`/`EmptyContentState` in place of the table, a `table-fixed` `DataTable` (Title/Workflow/Format/Created/Status columns, `Pill` for Format and Status), row actions collapsed to a primary "View preview" `TableIconButton` (eye icon) plus a `TableIconMenu` overflow (Mark read/unread, Delete), and a `ListPaginationFooter`. The "View preview" action now opens an `InboxDetailsModal` built the same way as `DataReadsHistoryTable.tsx`'s `ReadDetailsModal`/`IntegritasHistoryTable.tsx`'s `ProofDetailsModal` — a `DetailList` of facts (Workflow/Format/Created/Status) plus a `Disclosure` "Preview" section — instead of the old panel's always-expanded inline preview per row; the format-specific rendering (JSON via `JsonPreviewContent`, external link, image, plain text) is unchanged logic, just relocated into the modal and retoned onto ESDS type/color classes (was raw `slate-*`/`blue-*` Tailwind).
- Bumped the inbox fetch in `AutomationPage.tsx`'s `refresh()` from `listAutomationInbox({ status: "all", limit: 10 })` to `limit: 500` (the backend route's hard cap, confirmed via `limitFromQuery` in `automation.routes.ts`) so the new table's own client-side filter/search/pagination isn't silently truncated at the old fixed 10 — same "fetch everything once, paginate client-side" pattern the Workflows table (and `listAutomationWorkflows()`, which is already unbounded) already uses, rather than introducing a second, inconsistent server-driven-pagination pattern just for this one table (the backend does support real `limit`/`offset`/`status` pagination here, matching Diagnostics' tabs, but Diagnostics' query-string-driven multi-tab pagination is a materially heavier pattern that isn't otherwise used on this page — picked consistency with the sibling table over that instead).
- Wired inbox delete onto the same confirm-then-progress modal pattern as workflow delete: added `deleteInboxTarget`/`deletingInboxItem` state and `deleteInboxItem`/`confirmDeleteInboxItem` functions in `AutomationPage.tsx`, mirroring `deleteTarget`/`deletingWorkflow`/`deleteWorkflow`/`confirmDeleteWorkflow` exactly — previously "Delete" on an inbox item deleted immediately with no confirmation, the last such non-confirmed delete action left on the page. Added an `inboxLoading` state (true until the first `refresh()` resolves, alongside the existing `workflowsLoading`) passed as the new table's `loading` prop.
- Continued the drift pass with one more fix on the same page: the workspace ("build workflow"/"edit"/"watch") loading fallback still used raw `<section className={cardClass}><p className={mutedText}>Loading workflow...</p></section>` instead of the shared `LoadingState` already used everywhere else on this page now — swapped it in (`title="Fetching your workflow"`), which made the page's `cardClass`/`mutedText` import from `workflowWorkspaceUi.tsx` fully unused there (both exports are still used by `WorkflowWorkspace.tsx`/`WorkflowCanvas.tsx`/`WorkflowBlockInspectors.tsx`/`WorkflowWatchUi.tsx`, so nothing was removed from `workflowWorkspaceUi.tsx` itself) and removed the now-dead import.
- Ran `npx prettier --write` on the three touched/new files (`AutomationPage.tsx`, `AutomationWorkflowsList.tsx`, `AutomationInboxTable.tsx`) after `prettier --check` flagged formatting drift from manual edits; re-verified `npm run typecheck` and `npm --prefix frontend run build` afterward — both still clean.
- Re-verified with `npm run typecheck` (clean), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. `CHANGELOG.md` updated (same `[Unreleased] ui/pre-release-improvements` section). No manual browser check yet.
- User then asked for three more inbox refinements: make it a collapsible `Disclosure` (rather than an always-expanded `TableCard`), auto-mark items read on view so the manual "Mark read" press isn't needed, and asked whether to keep the eye icon or switch to mail/open-mail icons. Answered the icon question directly (recommended `Mail`/`MailOpen` reflecting read state, doubling as a status indicator, similar to Gmail-style inbox UX) since the other two were concrete requests, not open questions.
- Rebuilt `AutomationInboxTable.tsx`'s outer shell: swapped `TableCard` for a manually-composed `Card` + `Disclosure` (same combo `WorkflowValidationPanel`/`AuthSettingsPage.tsx`'s settings sections already use for a collapsible section with a `Pill` in the summary), `defaultOpen={false}` so it starts collapsed under the Workflows table, with the unread-count `Pill` staying in the `Disclosure`'s `title` (so it's visible even collapsed) instead of `TableCard`'s `actions` slot. Description/filter bar/table/pagination all moved inside as `Disclosure` children via `contentClassName="gap-detail-close grid"` to preserve the same spacing `TableCard` gave them.
- Added a `viewItem(item)` handler: opens the details modal (`setDetailsItem`) and, only if `!item.readAt`, also calls the existing `onMarkRead(item, true)` prop — reuses the page's existing mark-read plumbing (`AutomationPage.tsx`'s `run()` → `updateAutomationInboxItem` → `refresh()`) rather than adding a new code path. Left the overflow menu's manual "Mark read"/"Mark unread" toggle unchanged (still useful for reverting after viewing, or marking read in bulk without opening each item) rather than removing it now that the primary action covers the common case.
- Swapped the primary row action from a generic `Eye` "View preview" icon to `Mail` (unread)/`MailOpen` (read) from `lucide-react`, so the icon doubles as a per-row read-state indicator in addition to being the view action; title text also now hints "(marks as read)" for unread items. The table's existing "Status" column (Read/Unread `Pill`) is unchanged/left in place — not asked to remove it, and it stays useful for at-a-glance scanning at typical row density where the small action icon alone might be easy to miss.
- Noted but left alone: the details modal's own "Status" row reflects whatever `item.readAt` was at the moment the modal was opened (a snapshot, since `detailsItem` isn't re-derived from the live `items` prop after the mark-read call resolves) — same snapshot-on-open pattern every other detail/view modal in the app already uses (e.g. address book's `ContactDetailModal`), so it'll still show "Unread" until the modal is closed and reopened even though the row/header counts update correctly in the background. Not changed, since fixing it would mean deriving `detailsItem` by id-lookup into `items` instead of holding the object directly — a bigger behavioral change than asked for a cosmetic one-open lag.
- Verified with `npm run typecheck` (clean), `npx prettier --check` (clean), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. `CHANGELOG.md` updated (same `[Unreleased] ui/pre-release-improvements` section). No manual browser check yet.
- User flagged the details modal's "Status" `DetailRow` (added earlier this same follow-up, then immediately made stale by the auto-mark-read change above it) as redundant: since opening the modal always marks the item read now, it would only ever show "Read" — removed the row entirely rather than leaving a row that can no longer show anything but one fixed value. Re-verified with `npm run typecheck`, `npx prettier --check`, `npm --prefix frontend run build`, `docker compose config` — all pass.
- User asked to add the connected Integritas Connect user account ID to the Feedback system alongside the existing local device info. Added `metadata.integritasAccount.userId` (`string | null`) to the feedback document (`backend/src/features/feedback/feedback.service.ts`'s `FeedbackDocument`/`buildMetadata()`), read via the existing `getIntegritasAuth()` repository accessor (`integritas-auth.repository.ts`, `integritas_auth.integritas_user_id` column) — `null` when Integritas Connect isn't linked. Rebuilt fresh on every submission via the existing `buildMetadata()` call, same as `device`/`app`/`user`. Surfaced it in `FeedbackModal.tsx`'s `FeedbackExportDoc` type and "What we save with this feedback" disclosure as a new "Integritas account ID" row (shows "Not connected" when null), placed next to the existing "Device ID" row.
- Verified with `npm --prefix backend run build`, `npm --prefix frontend run build`, `npm run check` (typecheck + tests clean; `audit:moderate` fails only on pre-existing unrelated transitive-dep advisories), `docker compose config` — all pass. `CHANGELOG.md` updated under the same `[Unreleased] ui/pre-release-improvements` section. No manual browser check yet.
- User then hit a blank page opening the Feedback modal locally. Root cause: the running `backend`/`frontend` containers (`build:`-based in `docker-compose.yml`, not digest-pinned per `.claude/rules/update-agent.md`) predated this change, so `GET /api/feedback/export` didn't yet return `metadata.integritasAccount`, and `FeedbackModal.tsx` did `exportDoc?.metadata.integritasAccount.userId` — `.userId` on `undefined` threw during render. Hardened that access to `integritasAccount?.userId` (defends against any stale on-disk `feedback-submissions.json` too, not just this container-staleness case), then `docker compose build backend frontend` + `docker compose up -d backend frontend` — both healthy afterward.
- User then asked to add a flag tracking whether the local admin account's credential is a PIN or a password, so a later UI change can branch on it. Added `users.credential_type` (`ensureColumn`, `TEXT NOT NULL DEFAULT 'password'`, `database.ts`) and a new `getAdminCredentialType(credential)` helper (`password.service.ts`) that derives `"pin" | "password"` from the same validation regexes `isValidAdminCredential` already uses (a PIN is exactly 6 digits; a password requires upper/lower/number/symbol — the two patterns can't overlap, so no separate type parameter is needed at call sites). Threaded `credentialType`/`credential_type` through `UserRecord`/`SessionUser` (`auth.types.ts`), `createUser`/`updateUserPassword` (`auth.repository.ts`, both now require/persist it), `completeSetup()` (`setup.service.ts`, computed once from the submitted credential and passed to `createUser` and the returned `user` object), `login()`/`changePassword()` (`auth.service.ts`), `validateSession()` (`session.service.ts`), and `GET /api/auth/me` (`auth.routes.ts`) — so it's set at setup, updated on every credential change, and readable from both login and session responses. Added matching `credentialType: "pin" | "password"` to the frontend `AuthUser` type (`features/auth/types.ts`); `api.ts`'s `getMe()`/`login()` already consume it generically, no other frontend change needed since no UI reads the field yet (that's the deferred follow-up work).
- Verified with `npm run check` (typecheck + tests clean; `audit:moderate` fails only on the same pre-existing unrelated transitive-dep advisories noted earlier this session), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. `CHANGELOG.md` updated under the same `[Unreleased] ui/pre-release-improvements` section. No manual browser check (no UI change yet; existing PIN/password login untouched).
- Rebuilt/restarted the `backend` container from scratch (`docker compose build --no-cache backend` + `up -d --force-recreate backend`) to pick up the `credential_type` migration — confirmed healthy, `ensureColumn` backfilled the existing row with the `'password'` default with no manual step needed (existing PIN users just show as `"password"` until their next credential change recomputes the real type).
- User then asked to test the flag by updating in-app PIN/password inputs. Confirmed `PinField` (`components/ui/PinField.tsx`) was already a shared, reusable component (used by the setup wizard and login's TOTP step) — no extraction needed. Login-page auto-switching was flagged as needing a new pre-login lookup (the only public pre-auth endpoint, `GET /api/setup/status`, would have to start returning `credentialType`, a minor new disclosure) and deferred to post-v1 per the user's call. Built a shared `CredentialField` (`features/auth/CredentialField.tsx`, since deleted — see below) and wired it into every existing "Current PIN or password" re-auth prompt app-wide (`ChangeCredentialPanel`, `AuthSettingsPage`'s TOTP reset, all 5 `MinimaBackupPanel` re-auth modals, `MinimaConsoleWhitelistModal`), each reading `useAuth().user?.credentialType`. Also swapped `ChangeCredentialPanel`'s "New PIN"/"Confirm new PIN" fields from a numeric-constrained `InputField` to the same `PinField` the setup wizard uses, capped at `max-w-md` (uncapped `CredentialField` re-auth fields, per explicit user correction after briefly over-applying the cap everywhere).
- User then asked to revert almost all of that app-wide re-auth wiring (wanted more time to make it look right, deferred to post-v1) but keep the `ChangeCredentialPanel` "New PIN"/"Confirm new PIN" `PinField` swap, since that one didn't depend on the backend flag at all (driven by the form's own local toggle, not `user.credentialType`). Reverted `AuthSettingsPage`, `MinimaBackupPanel` (all 5 fields), `MinimaConsoleWhitelistModal`, and `ChangeCredentialPanel`'s own "Current PIN or password" field back to plain `InputField`; deleted the now-fully-unused `CredentialField.tsx` and its `useAuth` imports at each reverted call site. End state: the `credentialType` backend flag/plumbing from earlier this session is untouched and still live; the only surviving frontend change is `ChangeCredentialPanel`'s PIN-field reuse.
- Re-verified with `npm run typecheck`, `npm --prefix frontend run build`, `npm --prefix backend run build`, `npx prettier --check` (the 4 touched files), `docker compose config` — all pass. `CHANGELOG.md` updated (same `[Unreleased] ui/pre-release-improvements` section) with the one surviving UI change.

## Next Steps

- Manual browser check of the rebuilt Automation "Workflows" table: filter/search bar behavior (including disabling correctly per the earlier `ListFilterBar` empty-vs-filtered-empty fix), pagination, the pause/play primary action, the overflow menu's six actions (especially Delete now going through confirm → progress modal instead of deleting immediately), and the loading/empty states against the same Figma-driven pattern used elsewhere.
- Manual browser check of the rebuilt Automation inbox table: confirm it now renders full width like the Workflows table above it, starts collapsed as a `Disclosure` with the unread-count pill visible in the summary either way, expands/collapses correctly; filter (All/Unread/Read) + search behavior; the "View preview" modal for each of the four preview formats (text/json/link/image, especially the local-path image fetch via `/api/automation/inbox/:id/image`); clicking the row's `Mail`/`MailOpen` action marks an unread item read automatically (icon swaps, header/status pills update) without a separate "Mark read" press; the overflow menu's manual "Mark read"/"Mark unread" still works; and Delete going through confirm → progress modal instead of deleting immediately.
- Manual browser check of the "Send feedback" modal's new "Integritas account ID" disclosure row: shows "Not connected" when Integritas Connect isn't linked, and the real account ID once linked.
- Post-v1: build a proper UI that reads `credentialType` app-wide (login page auto-switch, re-auth prompts in `MinimaBackupPanel`/`MinimaConsoleWhitelistModal`/TOTP reset) — attempted this session and reverted pending a better look/feel pass; also decide on the login page's pre-auth lookup tradeoff (new field on public `GET /api/setup/status` vs. a manual PIN/password toggle) before building that piece.

## Notes / Open Questions

- `frontend/src/app/brand.ts` and `frontend/src/app/names.ts` both export an identical `APP_NAME = "Edge Studio"` constant and are imported from different call sites — pre-existing duplication, not touched this session (out of scope; flagged here for whoever consolidates it).
