# Session

Scratch log for the session in progress. Update it as you go; reset it when a session's work is done and merged. Not a changelog — see `CHANGELOG.md` for user-facing history.

## Progress

Branch `ui/global-style-realignment`. (Previous log entries here were for `feature/minima-resync-rescue-improvements`, already merged; reset below per this file's own reset-when-merged rule.)

- Swept the app for leftover "Integritas Pi" product-name references (the in-app brand had already been renamed to "Edge Studio" via `app/brand.ts`/`app/names.ts`, but many scattered strings hadn't caught up) and renamed them to "Edge Studio": TOTP issuer (`totp.service.ts`) and account label (`auth.constants.ts`, also fixing a stale pre-"Edge Studio" "Edge Workbench" placeholder duplicated in `LoginPage.tsx`/`TwoFactorStep.tsx`), device/ESP32/MQTT setup copy, `AutomationPage.tsx` example JSON bodies, `update-agent`'s page title, `install.sh` systemd `Description=` lines, and prose across `docs/guides/*.md`/`docs/plans/*.md`/`SECURITY.md`/`README.md`/both `add-device-support` `SKILL.md` copies.
- Left references to the repository/technical identity untouched, per explicit user instruction (to be handled later with the full repo rename): README/`docs/PROJECT.md` titles, the `integritas-pi` npm package names, the Docker Compose network (`integritas-pi`) and its "Integritas Pi Compose network/gateway" prose in `README.md`/`.claude`+`.agents`+`.cursor` `rules/docker.md(.mdc)`, and `integritas-pi-camera-helper`/`integritas-pi-sensor-helper` systemd unit/service identifiers (only their human-readable `Description=` text was changed).
- Fixed a stale brand-mark placeholder found while checking for old logo/icon usage: the onboarding wizard header (`OnboardingWizard.tsx`) rendered a generic Lucide `Layers3` icon instead of the actual logo; swapped it for the existing `BrandMark` component (already used correctly in the sidebar and login page, both pointing at `/es_logo/...` assets).
- User flagged that pre-"Edge Studio" brand names were also still live: found and fixed "Edge Workbench" (`SetupPage.tsx`, `IntegritasConnectPanel.tsx` x2, `OnboardingWizard.tsx`'s "Enter Edge Workbench" button label) and "Minima Edge Stack" (`SetupPage.tsx`) — same rename to "Edge Studio". Left `archive/mock/MinimaEdgeWorkbench.tsx` alone: confirmed it's outside `frontend/tsconfig.json`'s `include: ["src"]` and unreferenced, i.e. a dead historical mock, not live code — same treatment as past `CHANGELOG.md` entries, which describe history and aren't rewritten.
- Verified via `npm run check` (typecheck + backend tests + audit — audit failures are pre-existing transitive-dependency advisories, unrelated), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. No manual browser check this session (the changed onboarding-wizard mark is behind first-run setup state).
- Started a UI style-alignment pass: Account page's "Delete backup" flow (`MinimaBackupPanel.tsx`) used an ad-hoc single `Modal` with inline text/button, while the Devices page's device delete flow (`DataSourcesPage.tsx` + former `features/data-sources/DeleteDeviceModal.tsx`) used a nicer icon-centered confirm modal plus a separate non-closable progress modal. Extracted the device page's pair into a shared, generic `DeleteConfirmModal`/`DeleteProgressModal` (`components/patterns/DeleteConfirmModal.tsx`), deleted the device-specific file, and switched both `DataSourcesPage.tsx` and `MinimaBackupPanel.tsx` to use it — backup delete now matches the device delete UX (confirm modal closes immediately on confirm, then a progress modal shows until the delete call finishes). Verified with `npm run check`, `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass.
- User noticed a brief flash when the confirm modal swaps to the progress modal. Root cause was in the shared `Modal` (`components/ui/Modal.tsx`): it held a `mounted` state that started `false` and only flipped `true` in a post-commit `useEffect`, returning `null` on the first render otherwise. That's dead weight (no CSS transition was gated on it, and this is a pure client-side Vite app, no SSR hydration concern) — but it meant every newly-mounted `Modal` was invisible for one commit. When two modals swap in the same click handler (both state updates batch into one React commit), the old modal's portal disappears and the new modal's first render is still gated `null`, producing one paint with neither visible. Removed the `mounted` gate entirely (and the now-unused `useState` import) so `Modal` renders on its actual first render. This fixes the flicker for every modal-to-modal swap in the app, not just backup delete. Verified with `npm --prefix frontend run build`. Recorded the rationale in `docs/adr/0005-modal-mount-gate-removal.md` (not linked from `CHANGELOG.md`, per user request).
- Fixed a genuine contradiction found in the `adr` skill itself while doing the above: its own step 4 told the agent to leave a pointer comment in the affected code file, directly contradicting the skill's stated purpose ("ADRs exist so reasoning doesn't have to bloat code comments, commit messages, or changelog entries"). Removed that step from all three synced copies (`.claude/skills/adr/SKILL.md`, `.agents/skills/adr/SKILL.md`, `.cursor/skills/adr/SKILL.md`) and renumbered the remaining "report the file path" step; re-verified all three stayed byte-identical via `diff`.
- User flagged the Account settings page's Integritas/Minima status pills looked visually "drifted" from the header's status bar pills, with a screenshot. Investigated first (checked for raw non-`Pill` badges across `AuthSettingsPage.tsx` and its sub-panels — found none; both pills already used the shared `ui/Pill`), asked a clarifying question since the mismatch wasn't obvious from code alone, then the screenshot pinpointed it precisely: the header's `StatusBar` (`components/StatusBar.tsx`) renders its pills with `<Pill indicator>` (small status dot), while `AuthSettingsPage.tsx`'s two `Pill`s omitted `indicator` — same component, same tone system, just one missing prop. Added `indicator` to both. Verified with `npm --prefix frontend run build`.
- Restyled the "Send feedback" modal (`frontend/src/features/feedback/FeedbackModal.tsx`) onto shared ESDS components: `ui/Modal`, `ui/SelectField` (feedback type / area / bug severity / bug reproducibility / feature priority dropdowns), `ui/InputField` (expected/actual behavior, desired outcome), `ui/TextareaField` (description, now carrying the validation error via its own `error` prop instead of a separate error paragraph), `ui/Card` (current-page info box, bug/feature-request sub-sections), `ui/Button` + `patterns/ButtonRow`. Replaced the bespoke green "saved" success box and legacy `MutedText` (raw `text-slate-500`, not an ESDS token) with ESDS colour/type/spacing tokens throughout — same chrome pattern as `ErrorAlert` (white surface + status-coloured border + 20%-opacity feedback tint) but built locally since no shared "success" alert variant exists yet, to avoid growing `ErrorAlert`'s scope beyond what was asked. No behavior change. Verified with `npm run check`, `npm --prefix frontend run build`. Not manually checked in a browser this session — no browser-automation tool available in this environment; the app also needs an authenticated session to reach this modal (sidebar action), so this needs a manual pass.
- Added `LinkButton` (`components/ui/Button.tsx`): renders `Button`'s exact chrome on a real `<a>` instead of a `<button>`, for actions that must stay a link (e.g. file downloads) rather than a click handler. Needed its own `linkVariantClass` map instead of reusing `Button`'s `variantClass` verbatim — `Button`'s hover styling relies on the `enabled:hover:*` Tailwind variant, and `:enabled`/`:disabled` never match an `<a>`, so that hover would have silently never applied on a link. `danger` isn't offered (app-only, no anchor use case yet). Re-exported from the flat `components/Button.tsx` alongside `Button`/`IconButton`. Used it for the "Send feedback" modal's download link (replacing a one-off local class constant) and the new Account settings "Export feedback JSON" action below. Checked the rest of the frontend for other button-styled anchors worth converting — found none; the remaining raw `<a>` tags (`ChangelogPreview.tsx`, `AutomationPage.tsx`) are inline text links (`type-link`), a different, already-correct pattern.
- User asked for the Account settings page's "Check updates" action to move out of the page header into its own subsection, and a new subsection for browsing/exporting past feedback. Investigated the feedback backend first: V1 only has `POST /api/feedback` (create) and `GET /api/feedback/export` (whole-file download) — no list/edit/delete of individual submissions, and `docs/plans/feedback.md` explicitly defers an "operator-facing feedback history/export page" to "V3 and beyond." Proposed the full list/edit/delete version and confirmed scope before building; user clarified the actual goal was narrower — the existing "Send feedback" modal makes it awkward to get the export file without submitting a new feedback entry first, so they just wanted a direct export action, no list/edit/delete. Added two new `SubSection`s in "User settings" (above "Change PIN or password"): "Software update" (moved `Button onClick={() => navigate("/update")}` here from the page header `action`) and "Feedback" (`LinkButton href="/api/feedback/export"`, no new backend routes needed). Page header now only keeps "Sign out". Renamed the in-app Update page's title from "Update" to "Software update" to match. Verified with `npm run check`, `npm --prefix frontend run build`, `docker compose config`.
- User asked to add the brand logo to `update-agent`'s own static update-progress page (`update-agent/public/index.html`, served from that service's own `express.static` — separate container/image from `frontend`, no shared assets at runtime). Easy: `frontend/src/features/auth/LoginPage.tsx` already uses a plain `<img src="/es_logo/svg/es-lockup.svg">` (no React component wrapper) for the icon+wordmark "lockup" variant, so this was just copying that same SVG into `update-agent/public/es-lockup.svg` (already covered by the Dockerfile's existing `COPY public ./public`, no Dockerfile change needed) and adding an `<img class="logo">` above the card's status views, styled to match the page's existing plain-CSS token conventions. Verified with `npm --prefix update-agent run build`, `npm run check`, `docker compose config`.
- User also asked to move the Update page's "Update now" action out of its page-header `action` slot into the body, next to the "Update available"/version copy it acts on (mirroring the same header-declutter idea just applied to Account settings). Header `action` now only renders "Check again" once up-to-date (`undefined` otherwise, dropping the old always-present "Update now"/"Check again" either/or). "Update now" now sits in a `ButtonRow` right under the version description, only when `!upToDate`, which also simplified its `disabled` check (`starting` only — the surrounding block already requires `status` to render at all, so the old `!status ||` was redundant there). Verified with `npm run check`, `npm --prefix frontend run build`, `docker compose config`.
- Fixed the just-added `update-agent` logo: it was broken (404) because `frontend/vite.config.ts` (dev) and `frontend/nginx.conf` (prod) both strip the `/update` prefix before proxying to `update-agent`, so a root-absolute `<img src="/es-lockup.svg">` resolved against the frontend's own origin instead of `update-agent`'s — the pre-existing `<script src="/update/app.js">` already followed the correct `/update/`-prefixed convention, the new `<img>` just hadn't matched it. Also moved the logo above the card (new `.page` wrapper, `display: grid; justify-items: center; gap: 24px;`) instead of inside it, and sized it 48px tall to match the login page's lockup in the same 420px-card context, per user request. Rebuilt/restarted the `update-agent` container and verified end-to-end through the user's actual dev setup (`npm run dev:frontend` on port 3030 + Docker for the rest): direct container fetch, proxied fetch, and the live rendered page all confirmed correct.
- User asked for a "Not found" page routed as the app's default fallback (replacing the old silent redirect-to-dashboard on any unmatched path), plus a "Coming soon" page for the sidebar's existing `marketplace` nav item/`NavId` (already wired in `nav.ts`/`app/types.ts` with a "Coming soon" badge, but with no matching route — it silently fell through to the dashboard too). Given two Figma reference screenshots (a generic "Something went wrong" state and a "Coming soon" marketplace state) showing the same icon + title + description + button layout, extracted a new shared `StatusPage` pattern (`components/patterns/StatusPage.tsx`) modeled on the existing `DeleteConfirmModal`'s already-established icon/title/description/action skeleton (same `gap-detail-near grid place-items-center text-center` structure, just full-page instead of modal-body-sized). Built `NotFoundPage.tsx` (`SearchX` icon, "Page not found") and `MarketplacePage.tsx` (`ShoppingCart` icon, matching the nav item's own icon, "Coming soon") on top of it, both with a "Back to dashboard" action. Wired `/marketplace` as a real route and changed the catch-all `*` route from `<Navigate to="/dashboard" replace />` to `<NotFoundPage />` in `App.tsx`.
- Sanity-checked `DeleteConfirmModal` (`components/patterns/DeleteConfirmModal.tsx`) against the same two Figma references per user request: layout/spacing already matches the new `StatusPage` pattern closely (both trace back to the same icon+title+description+action skeleton), so the only change needed was retoning its `Trash2` icon from `text-icon-error` (red) to `text-icon-secondary` (neutral grey, matching the muted icon color in both Figma references) — the red "Delete" button (`variant="danger"`) is unchanged, so the confirm action still reads as destructive.
- Verified all of the above with `npm run check` (typecheck + backend tests + audit — audit failures remain the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config`.
- User reported three issues from a screenshot of the new `NotFoundPage`: (1) it wasn't actually centered in the page's visible space — root cause was `StatusPage`'s `min-h-[70vh]` guess, which centered content within an arbitrary 70% slice rather than the real remaining height below the header; fixed by making `AppShell`'s `<main>` a flex column with the routed-content wrapper as `flex-1 min-h-0`, and `StatusPage` now uses `h-full` against that instead of a fixed vh number. (2) The sidebar's "Marketplace" nav item still redirected to `/dashboard` — `AppShellSidebar.tsx` had a `id === "marketplace" ? "/dashboard" : ...` hardcode from before the route existed, never removed once `/marketplace` landed; removed the special case. (3) Added a "Something went wrong" fallback per the third Figma reference: new `ErrorBoundary` (`components/ErrorBoundary.tsx`, a standard React render-error boundary — the natural way this state gets reached, since the user wasn't sure how it would be routed) wraps the routed content inside `AppShell` (keyed by `pathname` so navigating away from a crashed page auto-recovers), so sidebar/header survive a page-content crash instead of a blank screen; shows the same `StatusPage` shape (`OctagonAlert` icon, "Reload page" action).
- Verified with `npm --prefix frontend run build`, `npm run typecheck`, `docker compose config`.
- User asked for three things: Account settings' three disclosure sections (User/Integritas/Minima settings) to start closed instead of open; a new "Behaviour settings" section below Minima settings with a toggle for "close modal when clicking outside it"; and to bring back `Modal`'s outside-click-to-close (previously disabled entirely after it closed unexpectedly), gated by that new setting, firing only on press (not click/release), plus a safety-zone buffer around the dialog edge if easy. Investigated the old bug via `git log -p`: the original implementation used `onClick={onClose}` on the backdrop with a child `stopPropagation()`, which is `click`-based (fires on mouseup) — a text selection or drag starting inside the dialog and releasing outside it could still resolve to a `click` on the backdrop, closing unexpectedly.
- User then redirected the persistence approach mid-task from a backend-persisted setting (the pattern every other Account-page toggle uses, e.g. Minima's `autoRestartEnabled` via `getSetting`/`saveSetting`) to browser `localStorage`, and asked for a small consistent system for it. Built `frontend/src/lib/localSettings.ts` (`createLocalBooleanSetting(key, defaultValue)` — a tiny reactive store: `get`/`set`/`subscribe`, safe to use with React's `useSyncExternalStore`, wrapped in try/catch for unavailable storage) and `frontend/src/lib/behaviourSettings.ts` (`closeModalOnOutsideClickSetting`, default `true`, i.e. re-enabled by default per the user's "want to add it again" framing).
- Rewrote `components/ui/Modal.tsx`'s outside-close: backdrop now uses `onMouseDown` (press, not click/release) and only closes when `event.target === event.currentTarget` (the mousedown landed directly on the backdrop, not a bubbled child), gated by `closeDisabled` and the new `closeOnOutsideClick` (read via `useSyncExternalStore`). For the safety zone, moved the existing `w-full`/`max-w-*` sizing off the dialog box and onto a new wrapper div (`p-detail-close w-full max-w-*`) between the backdrop and the dialog — the wrapper's 16px padding sits inside the max-width-capped centered column but outside the dialog's own box, so a press there hits the wrapper (not the backdrop) and doesn't close; this is a layout-normal, non-absolute restructuring, chosen specifically to avoid the stacking/z-index problems an absolutely-positioned "halo" approach would have caused (would paint over the dialog's own static-positioned content per CSS stacking order, breaking clicks on buttons/inputs inside).
- Deliberately kept `Modal` decoupled from `features/*` (matches the existing `components/ui/` convention of only importing from `lib/`, never `features/`) by putting the setting store in `lib/` rather than a React context in `features/settings/`.
- Wired the new "Behaviour settings" `Disclosure` into `AuthSettingsPage.tsx` (below Minima settings, `defaultOpen={false}` like the other three now are) with a `SubSection` + `SwitchField` ("Close modal when clicking outside it"), reading/writing `closeModalOnOutsideClickSetting` directly via `useSyncExternalStore`/`.set()`.
- Verified with `npm run check` (typecheck + backend tests + audit — audit failures are the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. No backend changes at all this round (setting is local-storage only, no new API route). No manual browser check yet.
- User flagged the "Integritas settings" disclosure's status `Pill` "pops in from nowhere" once loaded, unlike "Minima settings" (which already renders a neutral "Checking…" pill immediately, then swaps to the real status once `minimaState` resolves). Fixed by applying the same always-rendered pattern: `integritasKind ? integritasStatusTone[integritasKind] : "neutral"` / `integritasKind ? integritasStatusLabel[integritasKind] : "Checking…"`, instead of the pill only rendering once `integritasKind` was truthy. Verified with `npm --prefix frontend run typecheck`, `npm --prefix frontend run build`.
- User asked for another Behaviour setting: sidebar collapsed/expanded start state, since it "always starts open on refreshes." Investigated `AppShellSidebar.tsx`'s existing collapse logic: initial `collapsed` state came from a lazy `useState` initializer checking `window.matchMedia(EXPAND_MQ)` (1024px breakpoint), but a mount `useEffect` immediately calls the same `matchMedia`-driven `sync()` right after first paint anyway (also re-run on every breakpoint crossing via the `change` listener) — so the lazy initializer's value only ever survives one frame; manually toggling the sidebar was always ephemeral and never persisted. Added `sidebarStartCollapsedSetting` (`lib/behaviourSettings.ts`, default `false`, same `createLocalBooleanSetting` factory as the modal setting) and changed both the initializer and `sync()` to `!mq.matches || sidebarStartCollapsedSetting.get()` — below the 1024px breakpoint, narrow viewport space still always wins and forces collapsed (unchanged); at/above it, the setting now decides instead of a hardcoded "always expanded." Deliberately read via a plain `.get()` (not `useSyncExternalStore`) in `AppShellSidebar` — this is meant to govern the state at mount/breakpoint-crossing time only ("start" collapsed), not live-force the sidebar open/closed the instant someone flips the setting elsewhere while mid-session, which would fight with in-session manual toggling. Added a second `SubSection`/`SwitchField` ("Sidebar" / "Start sidebar collapsed") under the same "Behaviour settings" `Disclosure`, next to the existing "Modals" one — this one does use `useSyncExternalStore` so the Account-page toggle itself stays in sync with the stored value. Verified with `npm --prefix frontend run typecheck`, `npm --prefix frontend run build`, `docker compose config`.
- User asked to swap the Account settings page's two `ListDisclosure` usages (Minima "Backups" list in `MinimaBackupPanel.tsx`, "Peers" list in `MinimaPeerConnectionsSection.tsx`) for tables with a limited height instead of a collapsible disclosure. Rebuilt both as a height-limited, scrollable `DataTable` (`components/DataTable.tsx` flat re-export of `components/patterns/DataTable.tsx`, the project's canonical native-`<table>` pattern per `docs/frontend-design-system.md`'s "Tables And Lists" section) wrapped in `ui/ScrollArea` with `max-h-80` (same 320px cap the old `ListDisclosure` used internally), instead of `TableWrap`'s built-in min-height shell (wrong direction: that guarantees a minimum, not a cap). Backups table: File / Created / Actions columns, one primary `TableIconButton` (Download) + a `TableIconMenu` (Restore, Delete) in Actions, matching the design system's stated Actions-column convention; kept a small "Backups (n/max)" label above the table since the disclosure used to show that count in its summary. Peers table: single Address column, same "Peers (n)" label above. Per user request, did not delete `ListDisclosure` (`components/patterns/ListDisclosure.tsx`, now unused) — added a one-line comment marking it a possible-deprecation candidate since neither of its two callers exist anymore and its fate is undecided. Verified with `npm run check` (typecheck + backend tests + audit — audit findings are the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. No manual browser check (no browser tool available this session).
- User followed up with a screenshot showing an unwanted blank strip inside the Actions column of the new backup table, on both the Backups and Peers tables, and asked for the "Size"/"Created" backup columns to be split out and for "Created" to reuse whatever timestamp formatting other tables already use. Root-caused the blank strip to `ui/ScrollArea`'s `[scrollbar-gutter:stable]`, which unconditionally reserves scrollbar-width space on the inline-end side even when content isn't tall enough to actually scroll — exactly backwards from what was wanted here (these two tables usually hold only a handful of rows). Added an opt-out `stableGutter` prop (default `true`, so every other `ScrollArea` caller — `AutomationPage.tsx`, `WorkflowCanvas.tsx`, `ErrorDetails.tsx`, `JsonBlock.tsx`, `MinimaConsolePanel.tsx`, `Modal.tsx` — is unaffected) and set it `false` on both Minima tables; also swapped the bracket arbitrary-value class for the equivalent canonical Tailwind v4 utility (`scrollbar-gutter-stable`) per an IDE lint suggestion while touching that line. Split the Backups table's combined "{size} · {local+UTC string}" cell into separate "Size" and "Created" columns, and replaced the one-off local+UTC `formatCreatedAt` helper with the same `formatLocalDateTime` (`lib/time.ts`) + `<time dateTime=…>` pattern already used by `IntegritasHistoryTable.tsx`/`WalletHistoryPanel.tsx`/`AutomationRunsTable.tsx`/`DataReadsHistoryTable.tsx` (local-time-only display, no UTC string — matches those tables, a deliberate behavior narrowing from the old dual-format string). Verified with `npm --prefix frontend run typecheck`, `npm --prefix frontend run build`, `npm run check`, `npm --prefix backend run build`, `docker compose config` — all pass.
- User shared Figma references for a workflow-library empty state ("Create your first workflow" + button) and a matching fetching state ("Fetching your workflows" / "This should take a few seconds."), and asked for a reusable empty/loading pair plus a new spinner design. Since the referenced page is the in-progress Automation workflow list (`pages/AutomationPage.tsx`) the user is deliberately not touching on this branch, scoped the work to the shared components plus every *other* place in the app with an equivalent hand-rolled empty/loading pattern, per explicit user instruction to exclude the workflow/automation page.
- Found the exact icon-square + bold-title + description + optional-button empty-state markup, and a `LoadingDots`-in-a-centered-row loading state, already hand-duplicated verbatim between `WalletHistoryPanel.tsx` and `AddressBookPanel.tsx` — extracted both into new shared `components/patterns/EmptyContentState.tsx` (icon/title/description/optional action button) and `components/patterns/LoadingState.tsx` (centered spinner, optional bold title/description), then switched both call sites to them.
- Built the new spinner from the Figma reference (a radial-dash "activity indicator" look, not the existing CSS ring): new `components/ui/SpinnerAlt.tsx` wraps lucide's `Loader` icon with `animate-spin`, same `size`/`tone` API as the existing `Spinner`. Marked `components/ui/Spinner.tsx` `@deprecated` (kept for now, not deleted) and swapped its 4 real call sites (`ProgressModal.tsx`, `patterns/DeleteConfirmModal.tsx`, `features/update/ChangelogPreview.tsx`, `features/setup/steps/ConnectIntegritasStep.tsx`) to `SpinnerAlt`. Deliberately left the separate `LoadingDots` (bouncing-dots) component and its many inline-value-loading call sites untouched — a different, already-intentional pattern, not what was asked to be replaced.
- Applied `EmptyContentState`/`LoadingState` to three more real tables that had no first-load fetching feedback at all (silently showed "empty" during the initial fetch, indistinguishable from a genuinely empty list): Devices (`features/data-sources/DataSourcesList.tsx`, new `loading`/`onAddDevice` props wired from `DataSourcesPage.tsx` via the existing `capabilities === null` "not yet loaded" signal — no new state needed; empty-state action opens the existing "Add input source" flow), and Diagnostics' Integritas/Devices tabs (`features/integritas/IntegritasHistoryTable.tsx`, `features/data-reads/DataReadsHistoryTable.tsx`, new `loading`/`onClearFilters` props wired from `DiagnosticsPage.tsx` via a new `tabLoading` state toggled around the existing tab-load effect). Left Diagnostics' "Automation" tab (`AutomationRunsTable.tsx`) untouched — also automation-adjacent, out of scope per the same exclusion. Left `MinimaBackupPanel.tsx`/`MinimaPeerConnectionsSection.tsx`'s plain-text `EmptyTableState` alone — those are small (`max-h-80`) embedded scroll lists, not full section tables, where the new boxed empty state would be visually oversized; flagged this judgment call rather than silently skipping it.
- Documented `EmptyContentState`/`LoadingState`/`SpinnerAlt` (and `Spinner`'s deprecation) in `docs/frontend-design-system.md`'s shared-component inventory and detail sections (fixed a pre-existing unclosed code fence in the `CopyableCode` section while inserting immediately after it, since a valid new section requires closing it).
- User pushed back hard on the first pass: the components didn't match the Figma references at all. Two concrete misses — (1) both states were rendered as a row *inside* the existing table (a `<td colSpan>` under a still-visible, now-pointless header row), when the reference shows the panel replacing the table entirely; (2) the new spinner was just `lucide-react`'s `Loader` icon spun via `animate-spin` — a rotating icon, not the reference's look. Rebuilt both: `EmptyContentState`/`LoadingState` now render as a single bordered `surface-primary`/`stroke-secondary` panel (`contentStatePanelClass`, shared between the two, exported from `EmptyContentState.tsx`) and every call site now branches `loading ? <LoadingState/> : empty ? <EmptyContentState/> : <TableWrap>…real table…</TableWrap>` instead of stuffing either into a table row — `DataSourcesList.tsx`, `WalletHistoryPanel.tsx`, `AddressBookPanel.tsx`, `IntegritasHistoryTable.tsx`, `DataReadsHistoryTable.tsx` all restructured the same way, each also moving its pagination footer inside the "has rows" branch since there's nothing to page through in the other two states.
- Rebuilt `SpinnerAlt` as a static (non-rotating) SVG: 8 pins in the same positions as `lucide-react`'s `Loader` glyph, each with its own `spinner-pin` opacity keyframe (new `@keyframes` in `styles.css`) staggered by `animation-delay` so the "lit" pin visibly travels around the dial one at a time, like an analog activity indicator — not a spinning icon. User then asked for the travel direction to be clockwise (the initial stagger order went counter-clockwise); fixed by reversing the per-pin delay index.
- Also swapped the empty-state icons/copy on the corrected call sites to read as "first-run" CTAs matching the reference's "Create your first workflow" tone (e.g. Devices: "Connect your first device" / "New device" with a `Plus` icon primary button; Address book: "Save your first contact" / "New contact") instead of the flatter "No X yet" phrasing carried over from the original inline markup, while keeping "Clear filters" (secondary-variant button) for the filtered-empty case on all four filterable tables.
- Re-verified with `npm run check`, `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass.
- Verified with `npm --prefix frontend run build`, `npm run check` (typecheck + backend tests + audit — audit failures are the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `docker compose config` — all pass. No manual browser check yet.
- User shared a screenshot of the search+filter bar (from the out-of-scope Automation page, but illustrating a pattern shared by `components/patterns/ListFilterBar.tsx`) and asked for three fixes app-wide, explicitly excluding the Automation/workflow page again ("that one is another co-worker's responsibility"). (1) The filter dropdown's label was inconsistent per caller ("Status" on wallet history/Diagnostics, "Kind" on wallet assets) — removed the `filterLabel` prop entirely and hardcoded `"Filter"` in `ListFilterBar`, since every caller was overriding away from the already-"Filter" default anyway. (2) Search/filter/pagination controls were being hidden (not just left enabled) during loading/empty states in most tables — `ListPaginationFooter`/`Pagination` were nested inside the "has rows" branch of the loading/empty ternary in `DataSourcesList.tsx` and `WalletHistoryPanel.tsx`, so they vanished instead of just going inert. Moved both out to always render as siblings of the loading/empty/table branch, and passed `disabled={loading}` through. `AddressBookPanel.tsx`/`DiagnosticsPage.tsx` already had this right structurally, just needed `disabled` to also cover `tabLoading` (Diagnostics) in addition to the existing `refreshing`. `Pagination` itself had no real `disabled` prop before — passing `onPageChange={undefined}` disabled Previous/Next but not the numbered page buttons (their `onClick` was always a truthy wrapper function regardless), so added a proper `disabled` prop that suppresses all three, and `ListPaginationFooter` now passes it straight through instead of the old undefined-onPageChange trick. (3) The search input and filter select in `ListFilterBar` were hardcoded to `size="sm"` (`h-8`, `type-meta`) while regular form fields (`InputField`/`SelectField` elsewhere) default to `size="md"` (`h-[44px]`, `type-body`) — dropped the `size="sm"` override so they inherit the same `md` size as normal fields, removing the height/type-scale mismatch visible in the screenshot.
- While fixing the pagination-visibility issue, noticed `WalletAssetsPanel.tsx` (search+filter+table, same family as wallet history/address book) had never been migrated to the shared `LoadingState`/`EmptyContentState` panel from the earlier pass — it still used a bespoke `LoadingDots`-in-a-table-row loading state and an inline empty-row with its own "Clear filters" button. Migrated it onto the same `loading ? <LoadingState/> : empty ? <EmptyContentState icon={filtersActive ? Inbox : Coins} .../> : <TableWrap>…</TableWrap>` pattern as the other four tables, for consistency; its `ListFilterBar`/`ListPaginationFooter` were already correctly outside the ternary already, so no structural change needed there.
- Verified with `npm --prefix frontend run typecheck`, `npm run check` (typecheck + backend tests + audit — audit failures are the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. Confirmed via `git status` that `AutomationPage.tsx`/`AutomationRunsTable.tsx` remain untouched. No manual browser check yet.
- User asked for three more follow-ups on the same search/filter/pagination bar. (1) `ListFilterBar`'s search+filter fields now also disable once a table's (filtered) result is empty, not just while loading — added `|| <empty-condition>` to the existing `disabled` expression in `AddressBookPanel.tsx`, `WalletAssetsPanel.tsx`, `WalletHistoryPanel.tsx`, and `DiagnosticsPage.tsx` (the latter using the already-selected `activePager.items.length`, generic across its three tabs). (2) The small `variant="secondary" size="sm"` buttons sitting next to a search bar (address book "New contact", Diagnostics "Refresh") were swapped to default `Button` styling (`variant="primary"`/black, default `md` size) with a leading icon (`Plus`, `RefreshCw`) — these were the only two `ListFilterBar` call sites with an adjacent button; wallet assets/history have none. (3) User separately flagged the Diagnostics "Automation" tab (workflow run logs) was drifting from its "Integritas"/"Devices" siblings — unlike this session's earlier `AutomationPage.tsx`/`AutomationRunsTable.tsx` exclusion (that was about the workflow *canvas/builder* page, a co-worker's area), `AutomationRunsTable.tsx` is only ever rendered from `DiagnosticsPage.tsx` and the user explicitly asked for this one to be fixed. Added `loading`/`onClearFilters` props to `AutomationRunsTable.tsx` matching `IntegritasHistoryTable`/`DataReadsHistoryTable` exactly (`LoadingState` while loading, `EmptyContentState` with a "Clear filters" action when filtered-empty, replacing the old always-rendered table with an inline `<td colSpan>` "no rows" message and no loading state at all), removed the now-unused `colCount`/`compact` colSpan calc, and wired `loading={tabLoading}`/`onClearFilters` into `DiagnosticsPage.tsx`'s call site to match the other two tabs.
- Verified with `npm --prefix frontend run typecheck`, `npm run check` (typecheck + backend tests + audit — audit failures are the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. No manual browser check yet.
- User asked to disable the Wallet page's "Assets" tab for v1 (single native token makes a separate assets list redundant) — commented out (not deleted, matching the existing `CreateTokenModal`-disabled pattern in the same file) rather than removed: the `WalletAssetsPanel` import, the `{ value: "assets", label: "Assets" }` `TabList` option, and the `mainTab === "assets" ? <WalletAssetsPanel .../> :` branch in `WalletPage.tsx`. Default `mainTab` was already `"history"`, so the page still opens correctly with no reachable path to the disabled tab. Left the `WalletTab` type's `"assets"` union member and `WalletAssetsPanel.tsx` itself untouched, for an easy re-enable. Verified with `npm --prefix frontend run typecheck`, `npm --prefix frontend run build`.
- User asked to hide the Devices page's separate "Add devices" card (`DataSourcesPage.tsx`, the `<Card>` with "Add input source"/"Add output target" buttons above the device list) and instead wire a real filter+search bar on `DataSourcesList.tsx` with "New input"/"New output" buttons to its right, plus a second empty-state action matching that naming — then a follow-up asked to replace the list's existing top numbered-page `Pagination` control (previously just a bare prev/numbers/next row above the table, no search/filter at all) with that same filter+search bar rather than stacking both. Commented out the "Add devices" `Card` block (and its now-unused `Button`/`ButtonRow`/`Card` imports) in `DataSourcesPage.tsx`, matching the established comment-out-not-delete pattern from the Wallet Assets tab. `DataSourcesList.tsx` now owns local `direction`/`query`/`page` state: a `ListFilterBar` (direction filter — All/Input/Output/Capture, matching the table's existing "Direction" column values via `sourceDirection()` — plus search across name/type/endpoint) sits where the old `Pagination` was, with "New input" (primary) and "New output" (secondary) `Button`s to its right, both with a leading `Plus` icon; `onAddDevice` prop renamed to `onAddInput`/`onAddOutput`, wired from `DataSourcesPage.tsx`'s existing `setAddDeviceMode("input"/"output")`. Bottom `ListPaginationFooter`'s `total` now reflects the filtered `visibleItems`, matching how every other filterable table on this branch already works.
- Extended the shared `EmptyContentState` (`components/patterns/EmptyContentState.tsx`) with an optional second action (`secondaryActionLabel`/`secondaryActionIcon`/`secondaryActionDisabled`/`onSecondaryAction`, always rendered as `variant="secondary"`) rather than duplicating the empty-state markup locally in `DataSourcesList.tsx` — it was the only caller needing two actions (no matching device yet: "New input" + "New output"; existing single "Clear filters" action for the filtered-empty case is unaffected, all other five callers still pass only the primary action). Fixed one typecheck error from the new search filter (`sourceEndpoint()` can return `undefined` for some source types; `.toLowerCase()` needed `?? ""` first). Verified with `npm run typecheck`, `npm --prefix frontend run build`, `npm --prefix backend run build`, `docker compose config` — all pass. Confirmed via `git status` only the three expected files changed. No manual browser check yet.
- User shared a reference image showing 4 target sidebar icons (3 in a red box, 1 in a purple/blue box) and asked to update Minima/Wallet/Integritas/Automation to match. Matched each by comparing the reference shapes against installed `lucide-react` (v0.468.0) icon source: the top icon (center dot with symmetric wave arcs both sides) is `Radio`'s exact SVG path; the card icon (rectangle + one horizontal divider line) is `CreditCard`'s exact path; the plain shield outline (no checkmark) is `Shield`'s exact path; the double-right-triangle icon is `FastForward`'s exact path. Updated `frontend/src/app/nav.ts`: Minima `RadioTower`→`Radio`, Wallet `Wallet`→`CreditCard`, Integritas `ShieldCheck`→`Shield`, Automation `Workflow`→`FastForward` (Devices/Dashboard/Diagnostics/Marketplace/Account icons untouched, not mentioned).
- User then asked, mid-turn, for a follow-up icon swap: a reference image of a plain two-panel rectangle (no arrow) for "opening the sidebar when collapsed." Found the sidebar's collapse/expand toggle button in `components/AppShellSidebar.tsx` (`{collapsed ? <PanelLeftOpen size={16} /> : <Minimize2 size={16} />}`) — `PanelLeftOpen`'s SVG has an extra arrow path baked in on top of the same rectangle+divider shape, while plain `PanelLeft` (no arrow) is an exact match for the reference. Swapped only the collapsed-state icon (`PanelLeftOpen` → `PanelLeft`); the already-correct `Minimize2` expanded-state icon was untouched since it wasn't part of either reference. Verified with `npm run typecheck`, `npm --prefix frontend run build`, `npm --prefix backend run build`, `docker compose config` — all pass. No manual browser check yet.
- User flagged a real bug from two screenshots (Devices, "Configured devices"): typing a search that matched nothing correctly showed the "No matching devices" empty state, but the search input and filter dropdown themselves went disabled/greyed out at the same time — a dead end, since the only way to recover was the separate "Clear filters" button, not editing the search that was actively being typed. Root cause: every `ListFilterBar`'s `disabled` prop was computed from the *filtered* result's length (`visibleItems.length === 0` / `filteredEntries.length === 0` / `filteredHistory.length === 0` / `visibleAssets.length === 0`) instead of the underlying unfiltered list, so disabling was meant for "there's nothing to search/filter at all" but actually fired on "your current search/filter matched nothing" — exactly backwards, and blocking the one thing that could fix it. Fixed all four client-side-filtered call sites to key off the raw prop/state list instead: `DataSourcesList.tsx` (`items.length === 0`), `AddressBookPanel.tsx` (`entries.length === 0`), `WalletHistoryPanel.tsx` (`items.length === 0`), `WalletAssetsPanel.tsx` (`tokens.length === 0`). `DiagnosticsPage.tsx` is server-paginated (its `activePager` is already post-filter, no separate unfiltered count fetched), so used the existing `listFiltered` boolean instead: `disabled={refreshing || tabLoading || (!listFiltered && activePager.items.length === 0)}` — same effect (only disable when nothing exists and no filter is even active), no new query needed. The `EmptyContentState` empty-state panels themselves (which still correctly branch on the filtered result to show "No matching X"/"Clear filters") were not touched — only the `ListFilterBar` `disabled` prop was wrong. Verified with `npm run typecheck`, `npm --prefix frontend run build`, `npm --prefix backend run build`, `docker compose config` — all pass.
- User asked for a copy pass across every page and section (explicitly excluding the Automation page, "another co-worker's responsibility"/out of scope for this branch): tighten descriptions that were vague or over-narrative into short, direct statements of what the page/section is for. Surveyed all top-level `Page` `desc` props, `SubSection`/`TableCard`/`StatusPage` descriptions, and setup-wizard step copy across the app. Most copy was already direct and consistent (established pattern: one factual sentence, no marketing language) — found and fixed a handful of outliers: Dashboard's `desc` ("Your workspace for trusted data, proofs, automation and value flows at the edge." — vague marketing tagline) rewritten to name what's actually on the page (node/device status, wallet balance, recent activity); Minima's `desc` referenced a nonexistent "Raspberry Pi Edition" product tier (actual brand is `APP_NAME = "Edge Studio"`, tagline "Raspberry Pi" — `app/brand.ts`) — reworded to "this device"; Integritas' and Devices' `desc` tightened (redundant clause / asymmetric "then prepare" phrasing); Account settings' `desc` was missing the "Behaviour settings" section (interface preferences) added earlier this branch — added it so the description matches all four disclosure sections that actually exist; the onboarding wizard's `WelcomeStep` intro paragraph ("{APP_NAME} is a platform used to manage, secure, and monitor your devices. This setup wizard will guide you through the setup process." — generic platform-marketing copy plus a redundant restatement of the "Setup guide" heading right above it) rewritten to name the actual steps ahead (secure this device, connect Integritas), which also dropped its now-unused `APP_NAME` import; `DataSourcesList`'s "Configured devices" table description fixed a stray comma ("input sources, and output targets"); Marketplace's "Coming soon" description trimmed from two sentences to one. Flagged (did not edit) `pages/SetupPage.tsx` — a `/setup` route with no nav link anywhere in the app, apparently superseded by the real onboarding wizard (`features/setup/steps/`), likely dead code worth a separate removal decision rather than polishing its copy. Verified with `npm run check` (typecheck + backend tests + audit — audit findings are the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `npm --prefix frontend run build` — all pass. No manual browser check yet.
- User asked wherever `EmptyState` (`components/patterns/EmptyState.tsx`, a bare left-aligned icon/title/description with no border, undocumented in `docs/frontend-design-system.md`) was used, to swap in `EmptyContentState` instead, and deprecate `EmptyState` if so. Its only consumer was the Device details modal's "No health data"/"No preview" `Disclosure` bodies (`DataSourcesList.tsx`) — swapped both to `EmptyContentState`, then deleted `EmptyState.tsx` outright (zero remaining call sites) rather than leaving a deprecated stub, since nothing else in the codebase referenced it. Verified with `npm --prefix frontend run typecheck`/`build`, `npm run check`, `npm --prefix backend run build`, `docker compose config`.
- User then clarified the actual ask was about the Diagnostics page's "view"/"view error"/"view details" *modals* (Integritas "View payload", device reads' "View"/"View error", Automation's "View run"), not the outer list/table shell — each of the four domains (Devices' own reference modal plus the three Diagnostics tabs) rendered its detail dialog completely differently, and the Devices `DeviceDetailsModal` (`DataSourcesList.tsx`: a `DetailList` of key facts, then `Disclosure` sections wrapping `JsonPreviewContent`/`ErrorDetailPanel`/`EmptyContentState`) is the one being finalized as the target layout. Recommended converging the three Diagnostics modals onto that same composition (not a single rigid shared component, since the underlying data genuinely differs) rather than inventing a new one-size-fits-all dialog, and got explicit go-ahead before implementing.
- User also asked, mid-implementation, to make each Diagnostics tab's row actions consistent ("view button then kebab menu") and confirmed the data-reads "View"/"View error" split should become one action, matching what was already planned. Implemented all of it: `IntegritasHistoryTable.tsx`'s "View payload" (bare `Modal`+`JsonBlock`, disabled without a payload) became a `ProofDetailsModal` (`DetailList`: Timestamp/UID/Status/Data hash; `Disclosure` "Payload" → `JsonPreviewContent`/`ErrorDetailPanel`(`proof_error`)/`EmptyContentState`), always-enabled "View details" eye button, with "Verify"/"Download" moved off standalone buttons into a `TableIconMenu` kebab. `DataReadsHistoryTable.tsx`'s separate "View" (`JsonPreview`) / "View error" (`ErrorDetails`) links became one `ReadDetailsModal` (`DetailList`: Read time/Source/Trigger/Status/Hash/Integritas proof link; `Disclosure` "Preview" → preview/error/empty three-way, same as the Device modal's "Last preview" section) behind a single "View details" eye button (no kebab needed, no other row actions exist). `AutomationRunInspectModal.tsx` was rebuilt from a bespoke `Field`-row summary + raw-`JsonBlock` disclosures onto `DetailList` (Started/Trigger/Status/Duration/Blocks) + `Disclosure` sections using `ErrorDetailPanel` for both the run-level error and each failed block's error (previously a richer ad hoc Type/Native-details breakdown — deliberately simplified to match the same minimal Message+Raw pattern `ErrorDetailPanel`/`HealthErrorPanel` already use elsewhere, rather than keeping automation as the one richer outlier) and `JsonPreviewContent` for the full run-data disclosure; its trigger button relabelled "View run" → "View details" for consistency with the other two tabs.
- That refactor left two shared components with zero real remaining call sites: `components/patterns/ErrorDetails.tsx` (superseded by `ErrorDetailPanel`) and `components/patterns/JsonBlock.tsx` (superseded by `JsonPreviewContent`, already used internally by `JsonPreview.tsx` itself) — deleted both, and removed the resulting dangling `import { JsonBlock } from "./JsonBlock"` in `patterns/JsonPreview.tsx` (already dead/unused before this change, now unresolvable). Updated `docs/frontend-design-system.md`'s shared-component inventory and detail sections accordingly: replaced the `ErrorDetails` entry with a new `ErrorDetailPanel` one (documenting it as the shared embeddable error body behind all four "view details" modals), removed the `JsonBlock` section entirely, and fixed `JsonPreview`'s own doc text (it said "modal body uses `JsonBlock`", which was already stale — the component's modal body has used the local `JsonPreviewContent` all along). Noted, but deliberately left alone as pre-existing/unrelated dead code: a separate, never-imported legacy `components/ErrorDetails.tsx` (flat, non-ESDS, its own bespoke `slate-*`/`blue-*` styling) — same name as the deleted `patterns/` one but a different, already-orphaned file untouched by this change.
- Verified with `npm run check` (typecheck + backend tests + audit — audit findings are the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. No browser-automation tool available in this environment; confirmed the dev backend (already running from a prior session, port 3000) and frontend (already running, port 3030) were both reachable, but did not click through the three rebuilt modals in an actual browser.
- User asked to survey every modal in the app for button-placement inconsistency (some put Save/Cancel/Delete in the shared `Modal`'s `footer`, others rendered them inline in the scrollable body) and, after agreeing the rule should be "any button that acts on the modal/form as a whole (Save/Cancel/Delete/Confirm/wizard Back) goes in `footer`; only buttons scoped to one piece of body content (copy-to-clipboard, a guide step's own action) stay inline", asked to fix every instance plus align button variant usage so the pattern doesn't keep drifting. Audited via a research agent across every `<Modal>`/`<DeleteConfirmModal>` call site in `frontend/src`; confirmed already-correct examples (`MinimaConsoleWhitelistModal`, `AltAddDeviceFlow`, `IntegritasHistoryTable` bulk-delete, `MinimaPage` restart-confirm) established the convention: Cancel (`secondary`) first, primary/danger action last, `danger` reserved for destructive actions, wizard Back is `ghost` + leading `ArrowLeft` icon, submit buttons living outside a body `<form>` use the HTML `form={id}` attribute to stay wired to fields still inside the scrollable body.
- Fixed every body-button instance found against that convention: `components/patterns/DeleteConfirmModal.tsx` (Cancel/Delete now in `footer`, `Delete` gained the same `Trash2` `iconStart` `IntegritasHistoryTable`'s bulk-delete already used); `FeedbackModal.tsx` (both saved/form states' buttons moved to `footer`, reordered Cancel-before-primary to match the rest of the app); `CreateTokenModal.tsx`/`SendPaymentModal.tsx` (submit moved to `footer` via `form={id}`, both gained a `Cancel` button they previously lacked — only the modal's `X` could dismiss them before); `AddressBookPanel.tsx`'s `AddContactForm`/`EditContactForm` (each now owns its own `<Modal>` with a `footer`, instead of being rendered as a bare form inside a `<Modal>` in the parent — matches how `ContactDetailModal`/`MinimaConsoleWhitelistModal` already structure themselves) and its `ContactDetailModal` footer (dropped a redundant `ButtonRow` wrapper inside `footer`, which already flex-wraps); `ClassicAddDeviceFlow.tsx` (Back button + submit, previously split across the top and bottom of the scrollable body, both moved into `footer`); `DataSourcesPage.tsx`'s "Edit device" modal (submit moved to `footer`, gained a `Cancel` button); all five of `MinimaBackupPanel.tsx`'s modals (confirm download, set/manage/remove backup password, upload restore, row restore confirm) moved their single action button to `footer` and gained a `Cancel` button each.
- For `ClassicAddDeviceFlow.tsx`/`DataSourcesPage.tsx`'s edit-device modal, both share `DataSourceForm.tsx`, which previously rendered its own submit `Button` (with a long inline `disabled` validity expression) at the end of its fields — moving that button to the two callers' `footer`s without duplicating the validity logic meant extracting it into a new exported `isDataSourceFormValid(fields)` in `DataSourceForm.tsx` (removed the internal `Button`/`onSubmit`/`busy` props entirely), mirroring the existing `isAltDeviceFormValid` helper the newer `AltAddDeviceFlow`/`AltDeviceForm` pair already uses for the same "submit lives in the footer" pattern — confirmed the two validity checks are logic-identical before treating them as the same convention, but left them as two separate functions (`AltDeviceForm.tsx` untouched) rather than consolidating cross-module, since that file wasn't otherwise part of this change.
- Removed now-unused `ButtonRow` imports left behind by the above in `AddressBookPanel.tsx` and `MinimaBackupPanel.tsx`.
- Verified with `npm run check` (typecheck + backend tests + audit — audit findings are the same pre-existing transitive-dependency advisories), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass. No manual browser check yet.
- User flagged one more modal inconsistency found by inspection: Diagnostics' Automation-tab "view details" modal (`AutomationRunInspectModal.tsx`) has a footer "Close" button while its two siblings from the same earlier convergence pass (`ProofDetailsModal`/`ReadDetailsModal`) don't. Checked every pure view/read-only modal in the app (`AssetDetailModal`, `HistoryDetailModal`, `ReceiveAddressModal`, `DeviceDetailsModal`, `InboxPreviewModal`, plus those two Diagnostics siblings) — none of them have a footer `Close`, all rely on `X`/Escape/outside-click only. Recommended and applied the smaller fix: removed `AutomationRunInspectModal`'s footer `Close` button (and now-unused `Button` import) rather than adding one everywhere else, so it matches the established view-only-modal convention instead of both patterns coexisting. Verified with `npm --prefix frontend run typecheck`/`build`.
- User asked to audit `Modal`'s `bodyClassName` override (strips the default bordered/boxed `ScrollArea` card styling, used where content is short/fixed and won't scroll on desktop) for the same kind of drift as the footer-button pass. Audited every `<Modal>` call site in `frontend/src` (grepped both `bodyClassName` usages and every `<Modal` JSX site) and sorted them into two already-latent categories: (A) detail/view modals and genuinely variable-length content (asset/history/receive-address/proof/read/device/workflow-run details, inbox preview, send-feedback form, device add/edit forms, console command whitelist editor, device setup guide) correctly keep the bordered scroll pane since that content can legitimately grow tall; (B) short fixed-shape forms/confirms/progress spinners (delete confirm/progress, add-contact, Minima backup password/restore confirms, alt-flow device picker) correctly drop it via the override. Found 5 outliers structurally identical to a category-B sibling but missing the override: `components/ProgressModal.tsx` (same spinner shape as `DeleteConfirmModal.tsx`'s `DeleteProgressModal`), `AddressBookPanel.tsx`'s `EditContactForm` (same shape as its sibling `AddContactForm`), `CreateTokenModal.tsx`, `SendPaymentModal.tsx` (both short wallet forms, same shape as `AddContactForm`), and `MinimaConsoleWhitelistModal.tsx`'s "Confirm whitelist changes" password modal (same shape as `MinimaBackupPanel.tsx`'s confirm-password modals). Added `bodyClassName="min-h-0 flex-1"` to all 5. Verified with `npm --prefix frontend run typecheck`/`build`.
- User flagged the address book's "View contact" modal (`ContactDetailModal` in `AddressBookPanel.tsx`) as wired inconsistently: it let you switch into Edit or an inline Delete-confirm state from its own footer, duplicating the row's kebab menu ("Edit"/"Remove") — confusing and inconsistent with every other detail/view modal in the app (asset, history, receive address, proof/read/device details, workflow run), none of which have any action buttons of their own. Also asked to route contact deletion through the shared `DeleteConfirmModal` used elsewhere (Devices, Minima backups) instead of the bespoke inline delete state, so this doesn't keep drifting. Rewrote `ContactDetailModal` down to a pure `{ entry, onClose }` read-only view (address + notes only, no footer, no mode-switching) — same shape as `ReceiveAddressModal`/`AssetDetailModal`. Split the panel's single `entryAction: { entry, mode }` state into four: `viewEntry`, `editEntry` (kebab "Edit" now opens the already-extracted `EditContactForm` directly, no longer routed through the view modal), `deleteTarget`/`deletingEntry` (kebab "Remove" now opens the shared `DeleteConfirmModal`, whose `onConfirm` clears `deleteTarget` immediately and shows `DeleteProgressModal` until the API call finishes — the exact confirm-then-progress pattern `DataSourcesPage.tsx`/`MinimaBackupPanel.tsx` already use). Verified with `npm --prefix frontend run typecheck`/`build`, `npm run check`, `npm --prefix backend run build`, `docker compose config` — all pass.

## Next Steps

- Manual browser check of the onboarding wizard's brand mark (first-run setup, both the mid-flow and final dark-background views) to confirm `BrandMark` renders correctly in that spot.
- Manual browser check of the Account page's backup delete flow to confirm the new shared confirm/progress modal renders and behaves correctly there, and that the `Modal` mount-gate removal actually resolves the reported flicker.
- Manual browser check of the restyled "Send feedback" modal (all form states: bug / feature request / other type selection, validation error, saved/download state) for visual correctness.
- Manual browser check of the Account settings page's new "Software update" and "Feedback" subsections (button navigates to `/update`; export link downloads the feedback JSON) and the Update page's relocated "Update now"/"Check again" actions.
- Manual browser check of `update-agent`'s static update-progress page (`/update/`, during a real in-progress apply) to confirm the logo now renders (was a proxy-path 404), sits above the card, and reads as the right size — only verified via `curl`/direct fetch so far, not visually in a browser.
- Manual browser check of the new `/marketplace` "Coming soon" page and the `NotFoundPage` (visit any unmatched path, e.g. `/does-not-exist`) for visual correctness against the Figma reference (now centered against the real available height), and confirm the "Back to dashboard" action on both, plus the sidebar "Marketplace" link now actually navigating there.
- Manual browser check of the new `ErrorBoundary` "Something went wrong" fallback — no automated way to trigger a real render error from here; throw one manually in a page component during a check, confirm sidebar/header stay up, "Reload page" works, and navigating away after a crash auto-recovers (boundary is keyed by `pathname`).
- Manual browser check of the retoned `DeleteConfirmModal` icon (backup delete on Account, device delete on Devices) to confirm the icon now reads as neutral grey rather than red, with the red "Delete" button unchanged.
- Manual browser check of the Account settings page's three disclosures (User/Integritas/Minima settings) now starting collapsed, and the new "Behaviour settings" section/toggle below Minima settings.
- Manual browser check of `Modal`'s restored outside-click-to-close: pressing the backdrop closes the modal; a text selection that starts inside the dialog and is released outside it does NOT close it (the original bug); a press in the small buffer just outside the dialog edge does NOT close it (safety zone); turning the new setting off stops outside-press closing entirely while the X button/Escape still work; confirm the setting persists across a page reload (localStorage).
- Manual browser check of the Account settings page's Minima "Backups" and "Peers" tables (now `DataTable` + `ScrollArea` instead of `ListDisclosure`): confirm the scroll cap kicks in past ~4-5 rows, the Backups row actions (Download button, Restore/Delete overflow menu) still work, and the empty states ("None yet." / "No configured peers returned…") render correctly.
- Manual browser check of the new `EmptyContentState`/`LoadingState` panel (now replacing the table/list, not a row inside it) against the Figma references, and their five call sites: Devices table ("Connect your first device") and first-load fetching state (the empty-state action is now "New input"/"New output", see below — was "New device" when this check was first written); wallet send history ("No send activity yet") and address book ("Save your first contact") empty/loading states; Diagnostics' Integritas and Devices tabs' fetching state on tab switch/page/filter changes and empty/filtered-empty states ("Clear filters").
- Manual browser check of the rebuilt `SpinnerAlt` (static pin dial, pins lighting clockwise, not a spinning icon) at its 4 call sites (delete/progress modals, changelog preview loading, Connect Integritas setup step) for visual correctness against the Figma reference.
- Manual browser check of the `ListFilterBar` sizing/label fix and the now-always-visible-but-disabled search/filter/pagination controls during loading and empty states, across wallet history, address book, and Diagnostics' Integritas/Devices tabs. (Devices' old numbered `Pagination` control this item also referenced has since been replaced entirely by a `ListFilterBar`, see below; wallet assets tab is disabled for v1, no longer reachable to check.)
- Manual browser check that the "New contact"/"Refresh" buttons render full-size and black with a leading icon (address book, Diagnostics). (The `ListFilterBar` disable-on-empty behavior this item originally referenced was later found to be a UX bug — it disabled on a *filtered* empty result, trapping the user — and has since been corrected to only disable when the underlying list is truly empty; see below.)
- Manual browser check of the Diagnostics "Automation" tab's new loading spinner (on tab switch/page/filter changes) and empty/filtered-empty state with "Clear filters", now matching the "Integritas"/"Devices" tabs.
- Manual browser check that the Wallet page's "Assets" tab no longer appears (only "History"/"Address book") and the page still opens on "History".
- Manual browser check of the Devices page: the "Add devices" card is gone; the device list's new filter (All/Input/Output/Capture) and search (name/type/endpoint) bar works and sits where the old numbered pagination control was; "New input"/"New output" buttons to its right open the correct add-device flow; the empty state shows both "New input" and "New output" actions when there are no devices at all, and just "Clear filters" when a filter/search matches nothing; bottom pagination footer's total/page count reflects the filtered list, not all devices.
- Manual browser check of the updated sidebar icons (Minima/Wallet/Integritas/Automation, both expanded and collapsed sidebar widths) against the reference image, and the collapsed-sidebar "expand" button now showing the plain two-panel icon instead of the arrow variant.
- Manual browser check of the search/filter-disable bugfix on Devices, address book, wallet history, wallet assets, and Diagnostics (all three tabs): type a search or pick a filter that matches nothing — the search input and filter dropdown must stay enabled (so the query can be edited/cleared inline), not just the separate "Clear filters" button; then confirm they still correctly disable when the list has no data at all (e.g. a brand-new install with zero devices/contacts/sends).
- If/when the full repo rename happens, revisit the intentionally-untouched repo/technical identifiers listed above (package names, Docker network name, systemd unit names, README/PROJECT.md titles).
- Continue the broader "style align some stuff" pass across the app (this backup-delete reuse and the feedback modal were the first two items; more may follow).
- Manual browser check of the tightened page/section descriptions (Dashboard, Minima, Integritas, Devices, Account settings headers; Devices table description; onboarding wizard welcome step; Marketplace) for wording/fit at real viewport widths.
- Decide whether to remove the orphaned `pages/SetupPage.tsx` (`/setup` route, no nav link, superseded by the onboarding wizard) or keep it — flagged, not acted on.
- Manual browser check of the rebuilt Diagnostics "view details" modals (Integritas proof, device read, workflow run) for visual/behavioral correctness against the Devices reference modal: `DetailList` facts render correctly, `Disclosure` sections expand/collapse, the preview/error/empty three-way switch shows the right body for real data in each state, and the new eye-icon "View details" + kebab-menu row actions work (Integritas' "Verify"/"Download" now inside the kebab, including the "Verifying…" label mid-request).
- Manual browser check of the modal button-placement/footer pass: delete confirm (device/backup), send feedback (both states), create token, send payment (both now with a working Cancel), address book add/edit contact, both classic add-device flow steps (Back + submit), edit device, and all five Minima backup modals (confirm download, set/manage/remove backup password, upload restore, row restore confirm) — confirm buttons render in the footer (not the scrollable body), Cancel/primary/danger ordering and styling look consistent, and every `form={id}`-wired submit button still actually submits its form (Enter-to-submit in a field, and the footer button click) for the ones whose fields moved into a body `<form>`.
- Decide whether the now-fully-orphaned legacy `components/ErrorDetails.tsx` (flat, non-ESDS, zero importers, unrelated to the `patterns/ErrorDetails.tsx` deleted this session) should be removed — flagged, not acted on since it predates this change.
- Manual browser check of the `bodyClassName` scroll-pane pass: progress spinner modal, address book "Edit contact", "Create custom token", "Send payment", and Minima console "Confirm whitelist changes" now render unboxed (no border/background card) around their content, matching their category-B siblings, while still fitting without a scrollbar at normal desktop widths.
- Manual browser check of the address book contact-actions rework: "View contact" (eye icon) now shows a read-only modal with no Edit/Delete buttons; kebab "Edit" opens the edit form directly; kebab "Remove" opens the shared delete-confirm modal, then a progress modal, then the row disappears with a success toast.

## Notes / Open Questions

- `frontend/src/app/brand.ts` and `frontend/src/app/names.ts` both export an identical `APP_NAME = "Edge Studio"` constant and are imported from different call sites — pre-existing duplication, not touched this session (out of scope; flagged here for whoever consolidates it).
